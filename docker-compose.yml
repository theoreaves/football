services:
  bg-remove:
    build:
      context: ./bg-remove
    ports:
      - "8099:8080"
    environment:
      # Set this to something random; Laravel will send it as X-BG-Token
      BG_REMOVE_TOKEN: "${BG_REMOVE_TOKEN:-}"
    restart: unless-stopped
